apiVersion: v1
kind: Service
metadata:
  name: myservice
  labels:
    app: nginx
spec:
  ports:
  - protocol: TCP
    port: 80
  selector:
    app: nginx


#kubectl run -it --rm curl --image=nbrown/curl --restart=Never <your_cluster_ip>:80