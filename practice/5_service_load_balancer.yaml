apiVersion: v1
kind: Service
metadata:
  name: lb
spec:
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 80
      nodePort: 30500
  type: LoadBalancer

  #lb service is not working, why ?